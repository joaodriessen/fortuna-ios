import SwiftUI

// PlanetView removed from design — floating planets were too game-like.
// File kept to preserve Xcode project references.
// CosmicBackground no longer references this.

struct PlanetView: View {
    var body: some View {
        EmptyView()
    }
}
