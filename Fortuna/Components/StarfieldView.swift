import SwiftUI

// StarfieldView replaced by MinimalStarfield inside CosmicBackground.swift
// File kept to preserve Xcode project references.

struct StarfieldView: View {
    var body: some View {
        EmptyView()
    }
}
